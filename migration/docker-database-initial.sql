-- Optional initialization script for Postgres container
-- GORM AutoMigrate will create required tables at app startup.

-- You can add custom SQL here if needed, for example:
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

